# Containers

Given heights (e.g. `[1, 2, 8, 1, 0, 7, 4]`), pick two to form a container. Which container can hold the greatest volume? Width `*` min(heights). Greedy insight: move the shorter pointer inward.

## Acceptance Criteria


